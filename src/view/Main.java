package view;

import javax.swing.JOptionPane;

import controller.KillController;
import controller.RedesController;

public class Main {

	public static void main(String[] args) {
		/*
		RedesController rede = new RedesController();
		String[] options = { "IP", "PING", "X" };
		int n;
		do {
			n = JOptionPane.showOptionDialog(null, "Selecione as opçoes", "Menu", JOptionPane.YES_NO_CANCEL_OPTION,
					JOptionPane.DEFAULT_OPTION, null, options, options[1]);

			switch (n) {
			case 0:
				rede.ip();
				break;
			case 1:
				rede.ping();
				break;
			case 2:
				System.exit(0);
				break;
			}

		} while (n != 2);
		
		
		KillController killer = new KillController();
		killer.listaProcessos();
		killer.mataPid(15840);
		killer.mataNome("notepad.exe");
		*/

	}

}
